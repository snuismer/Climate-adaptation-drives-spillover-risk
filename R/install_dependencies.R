# Run the following line of R code to ensure that all packages needed
# to run the analyses in the "R" subdirectory are installed

install.packages(
  c(
    "tidyverse",
    "ggtext",
    "optimx",
    "scales",
    "rootSolve",
    "numDeriv",
    "latex2exp",
    "ggpubr",
    "tinytable"
    )
  )
