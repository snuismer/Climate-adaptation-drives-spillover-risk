## Respository contents:
- The `Lifespan vs. Spillover` subdirectory contains cleaned village-level data with derived
information on *Brucella* force of infection into the human population
- The `Model Fitting` subdirectory contains R scripts to make simulated data to test parameter recovery, fit the mathematical model to data (simulated or real), and plot results. 
